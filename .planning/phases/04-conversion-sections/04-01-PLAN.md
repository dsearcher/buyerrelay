---
phase: 04-conversion-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/Testimonials.tsx
  - src/app/page.tsx
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04]

must_haves:
  truths:
    - "3 testimonial cards auto-rotate in a carousel with visible navigation arrows"
    - "Each card has dark background (#0F172A) with gradient accent border"
    - "Each card shows quote icon, bold quote, circular author photo placeholder, name, title, and highlighted metric in accent color"
    - "Placeholder testimonials use realistic but conservative real estate metrics"
  artifacts:
    - path: "src/components/sections/Testimonials.tsx"
      provides: "Testimonial carousel section with auto-rotation and navigation"
      min_lines: 100
  key_links:
    - from: "src/components/sections/Testimonials.tsx"
      to: "src/app/page.tsx"
      via: "import and render after CRMIntegrations section"
      pattern: "import.*Testimonials"
---

<objective>
Build the Testimonials carousel section with 3 auto-rotating dark-background cards featuring gradient accent borders, quote icons, author details, and highlighted conversion metrics.

Purpose: Social proof with dollar amounts builds trust and convinces visitors the product delivers measurable results.
Output: Testimonials.tsx component wired into page.tsx
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@brand_assets/brand-guide.md
@src/lib/animations.ts
@src/components/ui/SectionWrapper.tsx
@src/components/animations/ScrollReveal.tsx
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Testimonials carousel component</name>
  <files>src/components/sections/Testimonials.tsx</files>
  <action>
Create a 'use client' Testimonials component with these specifics:

**Data:** 3 placeholder testimonials with realistic real estate metrics:
1. Quote about AI texting converting old leads, metric "$2.4M in pipeline reactivated", author "Sarah Mitchell, Team Lead, Keller Williams Orlando"
2. Quote about appointment setting saving hours, metric "34% more appointments booked", author "James Rodriguez, Broker, RE/MAX Coastal"
3. Quote about live transfers closing deals faster, metric "22-day avg close vs 45-day industry avg", author "Michelle Chen, Managing Partner, Compass NYC"

**Layout:**
- Use SectionWrapper with id="testimonials"
- Section heading: "What Agents Are Saying" with subtitle "Real results from real estate professionals using BuyerRelay"
- Carousel shows ONE card at a time on mobile, all 3 visible on desktop (flex row, highlight active)
- Left/right navigation arrows (styled buttons with SVG chevrons, pill-shaped, border color #E2E8F0, hover primary)
- Dot indicators below showing active card
- Auto-rotation every 5 seconds using useEffect + setInterval, pauses on hover

**Card design (each card):**
- Dark background: #0F172A
- Gradient accent border: 1px with linear-gradient(135deg, #0066FF, #7C3AED) -- use a wrapper div with gradient bg and 1px padding, inner div with #0F172A bg
- Border-radius: 16px (brand cards radius)
- Padding: 32px
- Quote icon: inline SVG quote mark in primary color (#0066FF), 24px
- Quote text: white (#F8FAFC), Outfit font, font-semibold, text-lg
- Author photo: 48px circle with bg-slate-600 placeholder (initials or generic user SVG)
- Author name: white, font-medium
- Author title: text-muted (#94A3B8), text-sm
- Highlighted metric: accent color (#7C3AED), font-bold, displayed as a separate badge/pill below quote

**Animation:**
- Wrap section in ScrollReveal for fade-in on scroll
- Card transitions use opacity + translateX for slide effect between cards
- Use motion/react for card transition animation (300ms, ease)

**Responsive:**
- Desktop (lg+): Show all 3 cards in a row, active card has full opacity + scale(1.02), inactive cards have opacity-60 + scale(0.95)
- Mobile: Show only active card, swipe-like navigation via arrows
  </action>
  <verify>
    <automated>cd /Users/dsearcher/projects/realestate && npx tsc --noEmit src/components/sections/Testimonials.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Testimonials component renders 3 dark cards with gradient borders, auto-rotates every 5s, navigation arrows and dots work, responsive layout</done>
</task>

<task type="auto">
  <name>Task 2: Wire Testimonials into page.tsx</name>
  <files>src/app/page.tsx</files>
  <action>
Import Testimonials from '@/components/sections/Testimonials' and add it to the page after CRMIntegrations and before the pricing placeholder section. Remove the existing placeholder `<section id="pricing" .../>` -- it will be replaced by the actual Pricing component in plan 04-03. Keep the CTA placeholder for now.

Page order after wiring:
Navbar > Hero > SocialProof > ProductOverview > FeaturesGrid > CRMIntegrations > Testimonials > (pricing placeholder) > (cta placeholder) > Footer
  </action>
  <verify>
    <automated>cd /Users/dsearcher/projects/realestate && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Testimonials renders on page between CRMIntegrations and pricing placeholder, build succeeds with no errors</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Testimonials section visible on page after CRM integrations
- 3 cards with dark backgrounds and gradient borders render correctly
</verification>

<success_criteria>
Testimonial carousel auto-rotates through 3 dark-background cards with gradient accent borders; each card shows quote, author, and highlighted metric; navigation arrows and dots are functional; section is responsive
</success_criteria>

<output>
After completion, create `.planning/phases/04-conversion-sections/04-01-SUMMARY.md`
</output>
