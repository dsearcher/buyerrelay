---
phase: 04-conversion-sections
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/Pricing.tsx
  - src/app/page.tsx
autonomous: true
requirements: [PRIC-01, PRIC-02, PRIC-03, PRIC-04, PRIC-05]

must_haves:
  truths:
    - "3 pricing tiers display: Starter ($299/mo), Professional ($499/mo), Enterprise (Custom)"
    - "Professional tier is highlighted as 'Most Popular' with gradient border accent"
    - "Each tier lists 5-7 features with checkmarks"
    - "'All plans include 14-day free trial' text appears below the tiers"
    - "Enterprise card shows 'Contact Sales' CTA instead of a price"
  artifacts:
    - path: "src/components/sections/Pricing.tsx"
      provides: "3-tier pricing cards section"
      min_lines: 120
  key_links:
    - from: "src/components/sections/Pricing.tsx"
      to: "src/app/page.tsx"
      via: "import and render in pricing section area"
      pattern: "import.*Pricing"
---

<objective>
Build the Pricing section with 3-tier cards (Starter, Professional, Enterprise), Professional highlighted with gradient border, feature lists with checkmarks, and free trial notice.

Purpose: Removes pricing ambiguity so visitors can self-select their tier and move toward booking a demo.
Output: Pricing.tsx component wired into page.tsx
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@brand_assets/brand-guide.md
@src/lib/animations.ts
@src/components/ui/SectionWrapper.tsx
@src/components/animations/ScrollReveal.tsx
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Pricing cards component</name>
  <files>src/components/sections/Pricing.tsx</files>
  <action>
Create a 'use client' Pricing component:

**Layout:**
- Use SectionWrapper with id="pricing"
- Section heading: "Simple, Transparent Pricing" (Outfit, H2 40px, -0.02em, text-primary)
- Subtitle: "Start converting more leads today. No setup fees, cancel anytime." (Inter, text-secondary)
- 3 cards in a row on desktop (grid cols-3, gap-8), stack on mobile

**Tier data:**

1. **Starter** - $299/mo
   - "For solo agents getting started"
   - Features: "Up to 100 leads/month", "AI text conversations", "Basic lead qualification", "CRM integration (1 platform)", "Email support"
   - CTA: "Start Free Trial" (outline/secondary button, pill-shaped)

2. **Professional** - $499/mo (HIGHLIGHTED)
   - "Most Popular" badge (gradient bg pill at top of card)
   - "For teams ready to scale"
   - Features: "Up to 500 leads/month", "AI text + voice calling", "Advanced qualification scoring", "CRM integration (unlimited)", "Live call transfers", "Priority support", "Custom scripts"
   - CTA: "Start Free Trial" (primary solid button, gradient bg, pill-shaped)

3. **Enterprise** - "Custom"
   - "For brokerages and large teams"
   - Features: "Unlimited leads", "AI text + voice + live transfers", "Custom AI training", "Dedicated account manager", "Multi-office support", "SLA guarantee", "API access"
   - CTA: "Contact Sales" (outline button, pill-shaped)

**Card styling:**
- Standard cards: bg-white, 16px border-radius, padding 32px, blue-tinted shadow-md
- Professional (highlighted): gradient border using wrapper div technique (outer div with linear-gradient(135deg, #0066FF, #7C3AED) bg, 2px padding, inner div bg-white, 16px border-radius), shadow-lg, slight scale(1.05) to elevate above others
- "Most Popular" badge: inline gradient bg pill, white text, font-semibold, text-sm, positioned at top center of card (negative margin to overlap card edge)

**Price display:**
- Dollar sign: text-lg, text-secondary
- Amount: Outfit, font-bold, text-5xl, text-primary
- "/mo": text-lg, text-secondary
- Enterprise: "Custom" in same styling as price amount

**Feature list:**
- Each feature: green checkmark SVG (16px, stroke #10B981) + text (Inter, text-secondary)
- Vertical spacing: gap-3 between items

**CTAs:**
- All buttons: full width, pill-shaped (border-radius 999px), py-3
- Primary (Professional): bg gradient(135deg, #0066FF, #7C3AED), text-white, hover opacity-90
- Secondary (Starter/Enterprise): border 2px #E2E8F0, text-primary, hover border-primary, hover text-primary

**Below tiers:**
- "All plans include a 14-day free trial" centered text
- Small shield icon + "No credit card required" in text-muted

**Animation:**
- Cards use staggerContainer + staggerItem for staggered fade-in on scroll
- Professional card has subtle hover scale (scaleOnHover from animations.ts -- use inline whileHover={{ scale: 1.02 }} to avoid TS conflict per Phase 3 decision)
  </action>
  <verify>
    <automated>cd /Users/dsearcher/projects/realestate && npx tsc --noEmit src/components/sections/Pricing.tsx 2>&1 | head -20</automated>
  </verify>
  <done>3 pricing cards render with correct tier data, Professional highlighted with gradient border and "Most Popular" badge, Enterprise shows "Contact Sales", free trial text below</done>
</task>

<task type="auto">
  <name>Task 2: Wire Pricing into page.tsx</name>
  <files>src/app/page.tsx</files>
  <action>
Import Pricing from '@/components/sections/Pricing' and add it to the page. Position after Comparison (or after CRMIntegrations if other plans haven't executed yet). Remove the `<section id="pricing" .../>` placeholder if still present.

Note: Multiple plans modify page.tsx in this wave. Read current state first, insert Pricing in correct position.
  </action>
  <verify>
    <automated>cd /Users/dsearcher/projects/realestate && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Pricing section renders on page in correct order, pricing placeholder removed, build succeeds</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- 3 pricing tier cards visible with correct prices and features
- Professional tier visually highlighted with gradient border
</verification>

<success_criteria>
Three pricing tiers display correctly with Starter ($299), Professional ($499, highlighted "Most Popular"), and Enterprise (Custom, "Contact Sales"); each lists features with checkmarks; "14-day free trial" notice below; responsive stacking on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/04-conversion-sections/04-03-SUMMARY.md`
</output>
