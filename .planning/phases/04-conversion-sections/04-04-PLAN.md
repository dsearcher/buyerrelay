---
phase: 04-conversion-sections
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/FinalCTA.tsx
  - src/app/page.tsx
autonomous: true
requirements: [CTA-01, CTA-02, CTA-03, CTA-04]

must_haves:
  truths:
    - "Dark background section with 'Ready to Convert More Leads?' headline"
    - "Lead capture form with Name, Email, Phone, and Team Size fields (placeholder, no backend)"
    - "Gradient 'Book Your Demo' submit button"
    - "Trust badges below form ('Trusted by 500+ agents', security signals)"
  artifacts:
    - path: "src/components/sections/FinalCTA.tsx"
      provides: "Final CTA section with lead capture form and trust badges"
      min_lines: 80
  key_links:
    - from: "src/components/sections/FinalCTA.tsx"
      to: "src/app/page.tsx"
      via: "import and render as last section before Footer"
      pattern: "import.*FinalCTA"
---

<objective>
Build the Final CTA section with dark background, "Ready to Convert More Leads?" headline, lead capture form (name, email, phone, team size), gradient submit button, and trust badges.

Purpose: Final conversion point -- captures leads who have scrolled the entire page and are ready to act.
Output: FinalCTA.tsx component wired into page.tsx
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@brand_assets/brand-guide.md
@src/lib/animations.ts
@src/components/ui/SectionWrapper.tsx
@src/components/animations/ScrollReveal.tsx
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build FinalCTA section with lead capture form</name>
  <files>src/components/sections/FinalCTA.tsx</files>
  <action>
Create a 'use client' FinalCTA component:

**Layout:**
- Use SectionWrapper with id="cta" and dark background
- Background: linear-gradient(180deg, #0F172A, #1E293B) per brand guide dark section gradient
- Add subtle radial gradient overlay for depth (radial-gradient at 50% 0%, rgba(0,102,255,0.1), transparent)
- Centered content, max-width ~600px for the form area

**Heading:**
- "Ready to Convert More Leads?" (Outfit, font-bold, H2 40px, -0.02em, text-white)
- Subtitle: "Join 500+ real estate professionals using AI to close more deals" (Inter, text-slate-300)

**Form (placeholder, no backend submission):**
- 4 fields in a 2x2 grid on desktop, stacked on mobile:
  1. Name (text input, placeholder "Your Name")
  2. Email (email input, placeholder "work@email.com")
  3. Phone (tel input, placeholder "(555) 123-4567")
  4. Team Size (select dropdown: "Just Me", "2-5 Agents", "6-20 Agents", "20+ Agents")

- Input styling: bg-white/10 (semi-transparent white on dark bg), border 1px rgba(255,255,255,0.2), text-white, placeholder text-slate-400, border-radius 12px (brand input radius), padding 12px 16px, focus:border-primary focus:ring-2 focus:ring-primary/20
- Select styling: same as inputs, with custom chevron icon or appearance reset

- Submit button: full width below form grid, "Book Your Demo" text
  - Background: linear-gradient(135deg, #0066FF, #7C3AED)
  - Text: white, font-semibold, text-lg
  - Border-radius: 999px (pill)
  - Padding: 16px
  - Hover: opacity-90 with transform scale(1.02)
  - Active: scale(0.98)
  - Add right arrow SVG icon after text

- Form onSubmit: preventDefault, no actual submission. Optionally show a brief "Thank you" state or just log to console.

**Trust badges below form:**
- Horizontal row of 3 trust signals, centered, gap-8
- Each: inline SVG icon (16-20px) + text in text-slate-400, text-sm
  1. Shield icon + "256-bit Encryption"
  2. Users icon + "Trusted by 500+ Agents"
  3. Clock icon + "14-Day Free Trial"

**Animation:**
- Wrap heading in ScrollReveal (fadeInUp)
- Form fades in with slight delay after heading (use fadeIn from animations.ts with custom delay)
- Trust badges stagger in below form

**Responsive:**
- Desktop: Form fields in 2x2 grid
- Mobile: All fields stack vertically, full width
  </action>
  <verify>
    <automated>cd /Users/dsearcher/projects/realestate && npx tsc --noEmit src/components/sections/FinalCTA.tsx 2>&1 | head -20</automated>
  </verify>
  <done>FinalCTA section renders dark bg with heading, 4-field form, gradient submit button, and 3 trust badges; form prevents default on submit; responsive layout works</done>
</task>

<task type="auto">
  <name>Task 2: Wire FinalCTA into page.tsx</name>
  <files>src/app/page.tsx</files>
  <action>
Import FinalCTA from '@/components/sections/FinalCTA' and add it to the page as the LAST section before Footer. Remove the `<section id="cta" .../>` placeholder if still present.

Note: Multiple plans modify page.tsx in this wave. Read current state first, insert FinalCTA in correct position (after Pricing, before Footer).
  </action>
  <verify>
    <automated>cd /Users/dsearcher/projects/realestate && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>FinalCTA renders as last section before Footer, CTA placeholder removed, build succeeds</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Dark CTA section visible as last content section before footer
- Form fields render with correct styling on dark background
- Gradient submit button visible
</verification>

<success_criteria>
Final CTA section has dark gradient background with "Ready to Convert More Leads?" headline; lead capture form with 4 fields renders correctly; gradient "Book Your Demo" button works (preventDefault); 3 trust badges display below form; responsive layout on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/04-conversion-sections/04-04-SUMMARY.md`
</output>
