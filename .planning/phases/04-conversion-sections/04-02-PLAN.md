---
phase: 04-conversion-sections
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/Comparison.tsx
  - src/app/page.tsx
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04]

must_haves:
  truths:
    - "Section heading reads 'Ditch the Status Quo' (Verse.ai pattern)"
    - "Two columns show 'With BuyerRelay' (green checkmarks) vs 'Without BuyerRelay' (gray X marks)"
    - "7-8 comparison rows contrasting AI-powered vs manual approaches"
    - "Columns animate by sliding in from left and right sides on scroll"
  artifacts:
    - path: "src/components/sections/Comparison.tsx"
      provides: "Comparison table section with dual-column layout"
      min_lines: 80
  key_links:
    - from: "src/components/sections/Comparison.tsx"
      to: "src/app/page.tsx"
      via: "import and render in conversion sections area"
      pattern: "import.*Comparison"
---

<objective>
Build the Comparison Table section with "Ditch the Status Quo" heading, two-column "With vs Without" layout using green checkmarks and gray X marks, with columns sliding in from sides on scroll.

Purpose: Highlights the cost of inaction -- visitors see the gap between AI-powered lead conversion and manual methods.
Output: Comparison.tsx component wired into page.tsx
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@brand_assets/brand-guide.md
@src/lib/animations.ts
@src/components/ui/SectionWrapper.tsx
@src/components/animations/ScrollReveal.tsx
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Comparison table component</name>
  <files>src/components/sections/Comparison.tsx</files>
  <action>
Create a 'use client' Comparison component:

**Layout:**
- Use SectionWrapper with id="comparison"
- Heading: "Ditch the Status Quo" (Outfit, font-bold, H2 size 40px, -0.02em tracking, text-primary)
- Subtitle: "See why top-producing agents are switching to AI-powered lead conversion" (Inter, text-secondary)
- Two side-by-side columns below heading

**Columns:**
- Left column: "With BuyerRelay" header with success/green accent (#10B981)
  - Background: white card with green-tinted left border (4px solid #10B981)
  - Each row: green checkmark SVG icon + text
- Right column: "Without BuyerRelay" header with muted/gray accent
  - Background: white card with gray-tinted left border (4px solid #E2E8F0)
  - Each row: gray X mark SVG icon + text in text-muted color

**Comparison rows (8 pairs):**
1. "Two-way AI text conversations" vs "One-way blast texts"
2. "24/7 instant lead response" vs "Leads wait hours for callback"
3. "AI qualifies before handoff" vs "Agents waste time on cold leads"
4. "Smart appointment scheduling" vs "Manual back-and-forth booking"
5. "Automated lead nurturing" vs "Leads fall through the cracks"
6. "Live call transfers to agents" vs "Voicemail tag for days"
7. "CRM auto-sync on every touch" vs "Manual data entry after calls"
8. "Consistent follow-up on every lead" vs "Cherry-picking only hot leads"

**Checkmark/X icons:** Inline SVGs, 20px, checkmark uses stroke #10B981, X uses stroke #94A3B8.

**Card styling:**
- Cards: bg-white (bg-elevated), 16px border-radius, padding 32px, blue-tinted shadow-md
- Row items: padding-y 12px, border-bottom #F1F5F9 between rows (not on last)
- Text: Inter, 16px, text-primary for "With" column, text-muted for "Without" column

**Animation:**
- Left column: use slideInLeft from animations.ts (slides from left on scroll)
- Right column: use slideInRight from animations.ts (slides from right on scroll)
- Wrap each column in motion.div with the appropriate animation variant
- Row items stagger in after column appears (staggerContainer + staggerItem)

**Responsive:**
- Desktop (md+): Two columns side by side with gap-8
- Mobile: Stack vertically, "With BuyerRelay" on top, "Without" below
  </action>
  <verify>
    <automated>cd /Users/dsearcher/projects/realestate && npx tsc --noEmit src/components/sections/Comparison.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Comparison component renders 2-column layout with 8 comparison rows, green checkmarks vs gray X marks, columns slide in from sides on scroll</done>
</task>

<task type="auto">
  <name>Task 2: Wire Comparison into page.tsx</name>
  <files>src/app/page.tsx</files>
  <action>
Import Comparison from '@/components/sections/Comparison' and add it to the page. Position it after Testimonials (which plan 04-01 adds -- if not yet present, add after CRMIntegrations). The Comparison section should appear between Testimonials and the pricing placeholder.

Note: Multiple plans modify page.tsx in this wave. Each plan should only add its own import and component rendering. The executor should read the current state of page.tsx first and insert accordingly.
  </action>
  <verify>
    <automated>cd /Users/dsearcher/projects/realestate && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Comparison renders on page in correct position, build succeeds</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Comparison section visible with two columns of checkmarks and X marks
- Columns animate in from sides on scroll
</verification>

<success_criteria>
"Ditch the Status Quo" comparison table displays 8 rows contrasting "With BuyerRelay" (green checks) vs "Without" (gray X marks), columns slide in from left/right on scroll, responsive stacking on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/04-conversion-sections/04-02-SUMMARY.md`
</output>
