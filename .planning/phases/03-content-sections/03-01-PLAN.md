---
phase: 03-content-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/SocialProof.tsx
  - src/app/page.tsx
autonomous: true
requirements: [SOCL-01, SOCL-02, SOCL-03]

must_haves:
  truths:
    - "Stat quotes scroll continuously in an infinite marquee without gaps or stutter"
    - "Integration logos scroll in a separate infinite marquee below the stats"
    - "Logos display grayscale by default and transition to color on hover"
  artifacts:
    - path: "src/components/sections/SocialProof.tsx"
      provides: "Social proof section with dual marquee tickers"
      min_lines: 80
  key_links:
    - from: "src/components/sections/SocialProof.tsx"
      to: "src/app/page.tsx"
      via: "import and render between Hero and next section"
      pattern: "import.*SocialProof"
---

<objective>
Build the Social Proof ticker section with auto-scrolling stat quotes marquee and integration logo marquee with grayscale-to-color hover.

Purpose: Establishes credibility immediately below the hero -- visitors see social proof before any feature detail.
Output: SocialProof.tsx component wired into page.tsx
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@brand_assets/brand-guide.md
@src/components/ui/SectionWrapper.tsx
@src/lib/animations.ts
@src/app/page.tsx
</context>

<interfaces>
<!-- Existing patterns the executor needs -->

From src/components/ui/SectionWrapper.tsx:
```typescript
interface SectionWrapperProps extends SectionProps {
  children: ReactNode;
  dark?: boolean;
  noPadding?: boolean;
}
export function SectionWrapper({ children, id, className, dark, noPadding }: SectionWrapperProps)
```

From src/lib/animations.ts:
```typescript
export const fadeInUp, fadeIn, slideInLeft, slideInRight, staggerContainer, staggerItem, scaleOnHover
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build SocialProof component with dual infinite marquee tickers</name>
  <files>src/components/sections/SocialProof.tsx</files>
  <action>
Create a 'use client' SocialProof component with two horizontal marquee strips:

**Stat quotes marquee (top):**
- Quotes array: "Appointments lifted by 20-50%", "$72M in transactions closed", "31% higher answer rate", "AI responds in under 5 seconds", "500+ agents trust BuyerRelay", "17% more qualified leads"
- Each quote rendered in a pill/badge style with brand accent styling
- CSS keyframe animation for infinite horizontal scroll (left to right continuous)
- Duplicate the array to fill the gap (render quotes twice side-by-side for seamless loop)
- Use `@keyframes marquee { 0% { transform: translateX(0); } 100% { transform: translateX(-50%); } }` pattern
- Animation duration ~30s for smooth, readable scroll
- Light background, subtle top/bottom border (border-border token)

**Logo marquee (bottom):**
- Logo names: Follow Up Boss, Sierra, CINC, BoomTown, HubSpot, Salesforce, kvCORE, Chime
- Since we don't have actual logo files, render each as a styled text badge with the company name in a rounded rectangle (font-semibold, text-text-secondary). This looks intentional and professional -- similar to "Trusted by" sections on many SaaS sites before they have partner permission for logos.
- Apply CSS `filter: grayscale(100%)` by default, `filter: grayscale(0%)` on hover with `transition: filter 300ms`
- Same infinite marquee CSS animation pattern, slightly slower (~40s)
- Duplicate array for seamless loop

**Layout:**
- Use SectionWrapper with noPadding={true} for edge-to-edge marquee feel, but add vertical padding (py-8 md:py-12) inside
- Add subtle gradient fade on left/right edges using `mask-image: linear-gradient(to right, transparent, black 10%, black 90%, transparent)` for polished look
- NO section heading -- this is a ticker strip, not a titled section

**Brand compliance:**
- Pill badges use bg-primary/5 with text-primary for stat quotes
- Blue-tinted shadows on logo badges (shadow-brand-sm token)
- Use Outfit font for stat numbers/metrics, Inter for company names
  </action>
  <verify>
    <automated>cd /Users/dsearcher/projects/realestate && npx tsc --noEmit src/components/sections/SocialProof.tsx 2>&1 | head -20</automated>
  </verify>
  <done>SocialProof.tsx renders two infinite marquee strips -- stat quotes on top, integration logos below. Logos are grayscale by default, color on hover. Both marquees scroll continuously without gaps.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SocialProof into page.tsx below Hero</name>
  <files>src/app/page.tsx</files>
  <action>
Import SocialProof component and place it immediately after the Hero component, before the placeholder sections.

Replace the existing page.tsx content to add the SocialProof import and render. Keep all existing imports (Navbar, Hero, Footer) and placeholder sections (how-it-works, features, pricing, cta) intact.

The order should be: Navbar > Hero > SocialProof > placeholder sections > Footer

Add the marquee keyframe animations to src/app/globals.css if not using inline styles:
```css
@keyframes marquee {
  0% { transform: translateX(0); }
  100% { transform: translateX(-50%); }
}
```
  </action>
  <verify>
    <automated>cd /Users/dsearcher/projects/realestate && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>SocialProof renders on the page between Hero and content sections. No TypeScript errors.</done>
</task>

</tasks>

<verification>
- `npm run dev` serves page with scrolling social proof tickers visible below hero
- Stat quotes scroll left continuously in an infinite loop
- Logo badges display grayscale, transition to color on hover
- No horizontal overflow on mobile (375px)
- TypeScript compiles without errors
</verification>

<success_criteria>
Social proof section is visible below the hero with continuously scrolling stat quotes and integration logo tickers. Logos are grayscale by default and show color on hover.
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-sections/03-01-SUMMARY.md`
</output>
