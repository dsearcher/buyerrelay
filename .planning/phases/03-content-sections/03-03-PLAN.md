---
phase: 03-content-sections
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/FeaturesGrid.tsx
autonomous: true
requirements: [FEAT-01, FEAT-02, FEAT-03, FEAT-04, FEAT-05]

must_haves:
  truths:
    - "Four feature cards display in a 2x2 grid on desktop"
    - "Cards stack single-column on mobile"
    - "Each card has icon, heading, description, benefit bullets, and screenshot placeholder"
    - "Cards animate with staggered fade-in on scroll"
    - "Hover state scales card to 1.02 with elevated shadow"
  artifacts:
    - path: "src/components/sections/FeaturesGrid.tsx"
      provides: "Features grid section with 4 animated feature cards"
      min_lines: 100
  key_links:
    - from: "src/components/sections/FeaturesGrid.tsx"
      to: "src/lib/animations.ts"
      via: "staggerContainer, staggerItem, scaleOnHover variants"
      pattern: "import.*staggerContainer|staggerItem|scaleOnHover"
    - from: "src/components/sections/FeaturesGrid.tsx"
      to: "src/components/ui/SectionWrapper.tsx"
      via: "wraps section content"
      pattern: "import.*SectionWrapper"
---

<objective>
Build the Features Grid section with 4 feature cards (AI Calling, AI Texting, Appointment Setting, Live Transfers) in a 2x2 grid with staggered animations and hover effects.

Purpose: Communicates BuyerRelay's core capabilities in a scannable, visually engaging format.
Output: FeaturesGrid.tsx component ready to wire into page.tsx
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@brand_assets/brand-guide.md
@src/components/ui/SectionWrapper.tsx
@src/components/animations/ScrollReveal.tsx
@src/components/animations/StaggerChildren.tsx
@src/lib/animations.ts
</context>

<interfaces>
From src/components/ui/SectionWrapper.tsx:
```typescript
export function SectionWrapper({ children, id, className, dark, noPadding }: SectionWrapperProps)
```

From src/components/animations/StaggerChildren.tsx:
```typescript
// StaggerChildren wraps children with staggered animation on scroll
```

From src/lib/animations.ts:
```typescript
export const fadeInUp, staggerContainer, staggerItem, scaleOnHover
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build FeaturesGrid with 4 animated feature cards</name>
  <files>src/components/sections/FeaturesGrid.tsx</files>
  <action>
Create a 'use client' FeaturesGrid component with id="features" using SectionWrapper:

**Section heading:**
- H2: "Everything You Need to Convert Leads" with brand typography (40px/Outfit/700/-0.02em)
- Subtitle: "BuyerRelay handles the entire lead journey -- from first contact to booked appointment" in text-text-secondary
- Center-aligned, ScrollReveal fade-in

**Features data array (4 items):**

1. **AI Calling**
   - Icon: phone SVG (inline, 28px, text-primary)
   - Heading: "AI-Powered Calling"
   - Description: "Instantly call every lead with a human-sounding AI agent that qualifies and books appointments 24/7."
   - Bullets: "Calls within 60 seconds of inquiry", "Natural conversation flow", "Handles objections intelligently", "Transfers hot leads to you live"

2. **AI Texting**
   - Icon: message/chat SVG (inline, 28px, text-primary)
   - Heading: "Intelligent Text Conversations"
   - Description: "Two-way AI texting that nurtures leads with personalized follow-ups until they're ready to meet."
   - Bullets: "Personalized to listing interest", "Smart follow-up cadence", "Understands natural language", "Seamless handoff to agent"

3. **Appointment Setting**
   - Icon: calendar SVG (inline, 28px, text-primary)
   - Heading: "Automated Appointment Setting"
   - Description: "Qualified leads get booked directly onto your calendar -- no back-and-forth scheduling."
   - Bullets: "Syncs with your calendar", "Sends confirmation and reminders", "Reschedules automatically", "Pre-qualifies before booking"

4. **Live Transfers**
   - Icon: arrow-right-left/switch SVG (inline, 28px, text-primary)
   - Heading: "Live Call Transfers"
   - Description: "When a lead is ready to talk, BuyerRelay transfers them directly to you with full context."
   - Bullets: "Warm transfer with lead summary", "Real-time availability check", "No missed opportunities", "Full conversation history"

**Card layout:**
- Grid: 2 columns on desktop (md:grid-cols-2), 1 column on mobile
- Gap: 24px (gap-6)
- Each card: bg-bg-elevated, rounded-[16px], p-8, shadow-brand-md, border border-border
- Card structure (top to bottom): icon (in a 48px rounded-[12px] bg-primary/5 container) > heading (H3, 20px/Outfit/700) > description (text-text-secondary, 15px) > bullets (list with small blue checkmark SVG before each, text-text-secondary)
- Bottom of each card: a placeholder screenshot area (aspect-[4/3], rounded-[12px], bg-gradient-to-br from-primary/5 to-accent/5) with centered text "Screenshot" in text-text-muted -- this holds space for real product screenshots

**Animations:**
- Use motion.div with staggerContainer variant on the grid
- Each card wrapped in motion.div with staggerItem variant
- Apply scaleOnHover to each card for hover: scale 1.02 + shadow-brand-lg transition
- Add `transition: box-shadow 300ms` CSS for smooth shadow elevation on hover

**Brand compliance:**
- Cards use 16px radius, 32px padding
- Blue-tinted shadows (shadow-brand-md)
- Icon containers use primary/5 background
- Checkmark bullets in text-primary color
- Tight letter-spacing on card headings
  </action>
  <verify>
    <automated>cd /Users/dsearcher/projects/realestate && npx tsc --noEmit src/components/sections/FeaturesGrid.tsx 2>&1 | head -20</automated>
  </verify>
  <done>FeaturesGrid.tsx renders 4 feature cards in 2x2 grid with icons, headings, descriptions, bullet points, and screenshot placeholders. Cards have staggered scroll animation and hover scale effect.</done>
</task>

<task type="auto">
  <name>Task 2: Wire FeaturesGrid into page.tsx replacing features placeholder</name>
  <files>src/app/page.tsx</files>
  <action>
Read current page.tsx, add import for FeaturesGrid, replace the `<section id="features" className="min-h-[200px]" />` placeholder with `<FeaturesGrid />`.

Preserve all existing imports and sections. Order: Navbar > Hero > (SocialProof if present) > (ProductOverview if present) > FeaturesGrid > remaining placeholders > Footer
  </action>
  <verify>
    <automated>cd /Users/dsearcher/projects/realestate && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>FeaturesGrid renders on the page at the features anchor. No TypeScript errors.</done>
</task>

</tasks>

<verification>
- `npm run dev` shows Features Grid section with 4 cards in 2x2 layout
- Cards have staggered fade-in animation on scroll
- Hover on any card shows scale 1.02 + elevated shadow
- Mobile (375px): cards stack single-column
- Each card displays icon, heading, description, and benefit bullets
</verification>

<success_criteria>
Features grid displays 4 cards (AI Calling, AI Texting, Appointment Setting, Live Transfers) in a responsive 2x2 grid. Cards animate on scroll with stagger, have hover elevation effects, and include all required content (icon, heading, description, bullets, screenshot placeholder).
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-sections/03-03-SUMMARY.md`
</output>
