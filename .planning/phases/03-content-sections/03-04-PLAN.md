---
phase: 03-content-sections
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/CRMIntegrations.tsx
autonomous: true
requirements: [CRM-01, CRM-02, CRM-03]

must_haves:
  truths:
    - "Left column shows heading, subtitle, and 4 use-case badges"
    - "Right column shows a product screenshot placeholder for CRM sync timeline"
    - "Below the 2-column layout, an 8-logo integration grid displays"
  artifacts:
    - path: "src/components/sections/CRMIntegrations.tsx"
      provides: "CRM integrations section with use-case badges, screenshot, and logo grid"
      min_lines: 80
  key_links:
    - from: "src/components/sections/CRMIntegrations.tsx"
      to: "src/components/ui/SectionWrapper.tsx"
      via: "wraps section content"
      pattern: "import.*SectionWrapper"
    - from: "src/components/sections/CRMIntegrations.tsx"
      to: "src/lib/animations.ts"
      via: "animation variants for scroll reveal"
      pattern: "import.*slideInLeft|slideInRight|staggerContainer"
---

<objective>
Build the CRM Integrations section with a 2-column layout (use-case badges + screenshot placeholder) and an 8-logo integration grid below.

Purpose: Shows prospects that BuyerRelay works with their existing CRM and handles their specific lead scenarios.
Output: CRMIntegrations.tsx component ready to wire into page.tsx
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@brand_assets/brand-guide.md
@src/components/ui/SectionWrapper.tsx
@src/components/animations/ScrollReveal.tsx
@src/lib/animations.ts
</context>

<interfaces>
From src/components/ui/SectionWrapper.tsx:
```typescript
export function SectionWrapper({ children, id, className, dark, noPadding }: SectionWrapperProps)
```

From src/lib/animations.ts:
```typescript
export const fadeInUp, fadeIn, slideInLeft, slideInRight, staggerContainer, staggerItem
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build CRMIntegrations with use-case badges, screenshot, and logo grid</name>
  <files>src/components/sections/CRMIntegrations.tsx</files>
  <action>
Create a 'use client' CRMIntegrations component using SectionWrapper (no specific id needed -- this section is not a nav target, but use id="integrations" for future linking):

**Top section - 2-column layout:**

Left column (slideInLeft animation):
- Eyebrow text: "INTEGRATIONS" in uppercase, text-primary, font-semibold, text-sm, tracking-widest
- H2: "Works With Your CRM" (40px/Outfit/700/-0.02em)
- Subtitle: "BuyerRelay plugs into your existing tech stack. Import leads from any source and sync results back automatically." in text-text-secondary
- 4 use-case badges in a 2x2 grid (gap-3):
  1. "Old Leads" -- icon: clock/history
  2. "Rehashed Leads" -- icon: refresh/cycle
  3. "New Leads Off Hours" -- icon: moon/night
  4. "Uncontacted Leads" -- icon: user-x/missed
- Badge styling: bg-bg-elevated, rounded-full (pill), px-5 py-2.5, border border-border, shadow-brand-sm, flex with small icon (16px, text-primary) + label text (font-medium)
- Badges should feel interactive: subtle hover scale (1.02)

Right column (slideInRight animation):
- Product screenshot placeholder: aspect-[4/3], rounded-[16px], bg-gradient-to-br from-primary/5 to-accent/5, shadow-brand-lg
- Inside placeholder: centered text "CRM Sync Timeline" in text-text-muted with a simple timeline icon sketch (3 horizontal lines with dots)
- This holds space for the actual product/app/screenshots/crm-sync.png once available

Desktop: 2 columns (lg:grid-cols-2, gap-12, items-center). Mobile: stacked (left content first, screenshot below).

**Bottom section - Logo grid:**
- Centered heading: "Connects with 20+ platforms" in text-text-secondary, text-sm, font-medium, mb-8
- 2-row grid of 8 integration logos (grid-cols-4 on desktop, grid-cols-2 on mobile):
  Row 1: HubSpot, Follow Up Boss, BoomTown, Salesforce
  Row 2: Sierra, kvCORE, Firepoint, Chime
- Each logo: rendered as styled text in a card (bg-bg-elevated, rounded-[12px], p-4, border border-border, text-center)
- Text styled: font-semibold, text-text-secondary, text-sm
- Hover: border-primary/30 transition, subtle scale
- Staggered fade-in animation using staggerContainer/staggerItem

**Brand compliance:**
- Blue-tinted shadows on all elevated elements
- Pill badges for use cases (rounded-full)
- 16px radius on cards and screenshot placeholder
- Proper spacing scale (gap-12 between columns, gap-6 in logo grid)
  </action>
  <verify>
    <automated>cd /Users/dsearcher/projects/realestate && npx tsc --noEmit src/components/sections/CRMIntegrations.tsx 2>&1 | head -20</automated>
  </verify>
  <done>CRMIntegrations.tsx renders 2-column layout with use-case badges and screenshot placeholder on top, 8-logo integration grid below. Animations trigger on scroll.</done>
</task>

<task type="auto">
  <name>Task 2: Wire CRMIntegrations into page.tsx after features section</name>
  <files>src/app/page.tsx</files>
  <action>
Read current page.tsx, add import for CRMIntegrations, and insert `<CRMIntegrations />` after the features section (FeaturesGrid or its placeholder) and before the pricing placeholder.

Preserve all existing imports and sections. The CRM section sits between Features and the conversion sections (Phase 4).

Also clean up: remove any remaining Phase 3 placeholder sections if all 4 components are now present. Keep the pricing and cta placeholders for Phase 4.
  </action>
  <verify>
    <automated>cd /Users/dsearcher/projects/realestate && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>CRMIntegrations renders on the page after features. No TypeScript errors. Page shows all Phase 3 sections in order.</done>
</task>

</tasks>

<verification>
- `npm run dev` shows CRM Integrations section with 2-column layout and logo grid
- Use-case badges display as pills with icons
- Screenshot placeholder shows gradient background
- 8 integration logos display in 4-column grid on desktop, 2-column on mobile
- Animations (slideInLeft, slideInRight, stagger) trigger on scroll
</verification>

<success_criteria>
CRM section renders with left column (heading, subtitle, 4 use-case badges) and right column (screenshot placeholder), plus an 8-logo integration grid below. All elements animate on scroll.
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-sections/03-04-SUMMARY.md`
</output>
