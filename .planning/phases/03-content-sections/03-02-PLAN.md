---
phase: 03-content-sections
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/ProductOverview.tsx
autonomous: true
requirements: [PROD-01, PROD-02, PROD-03]

must_haves:
  truths:
    - "Section displays 'See How It Works' heading with descriptive subtitle"
    - "Video placeholder shows a compelling thumbnail area with a play button overlay"
    - "Three stat cards count up from zero to their values when scrolled into view"
  artifacts:
    - path: "src/components/sections/ProductOverview.tsx"
      provides: "Product overview section with video placeholder and stat cards"
      min_lines: 80
  key_links:
    - from: "src/components/sections/ProductOverview.tsx"
      to: "src/components/animations/CountUp.tsx"
      via: "import CountUp for stat card animations"
      pattern: "import.*CountUp"
    - from: "src/components/sections/ProductOverview.tsx"
      to: "src/components/ui/SectionWrapper.tsx"
      via: "wraps section content"
      pattern: "import.*SectionWrapper"
---

<objective>
Build the Product Overview section with "See How It Works" heading, video placeholder with play button, and 3 animated stat cards that count up on scroll.

Purpose: Shows visitors what BuyerRelay does with a visual demo area and compelling metrics.
Output: ProductOverview.tsx component ready to wire into page.tsx
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@brand_assets/brand-guide.md
@src/components/ui/SectionWrapper.tsx
@src/components/animations/CountUp.tsx
@src/components/animations/ScrollReveal.tsx
@src/lib/animations.ts
</context>

<interfaces>
<!-- Existing components the executor needs -->

From src/components/ui/SectionWrapper.tsx:
```typescript
export function SectionWrapper({ children, id, className, dark, noPadding }: SectionWrapperProps)
```

From src/components/animations/CountUp.tsx:
```typescript
// CountUp component that animates a number from 0 to target on scroll into view
// Uses Motion animate() for React 19 compatibility
```

From src/components/animations/ScrollReveal.tsx:
```typescript
// ScrollReveal wraps children with viewport-triggered animation
```

From src/lib/animations.ts:
```typescript
export const fadeInUp, fadeIn, staggerContainer, staggerItem
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build ProductOverview with video placeholder and count-up stat cards</name>
  <files>src/components/sections/ProductOverview.tsx</files>
  <action>
Create a 'use client' ProductOverview component with id="how-it-works" using SectionWrapper:

**Section heading:**
- H2: "See How It Works" with brand typography (40px/Outfit/700/-0.02em)
- Subtitle: "Watch BuyerRelay convert a cold lead into a booked appointment in under 2 minutes" in text-text-secondary
- Center-aligned, use ScrollReveal for fade-in

**Video placeholder (center):**
- Large rounded rectangle (aspect-video, rounded-[16px]) with brand gradient background (135deg blue to purple at low opacity ~10%)
- Centered play button: white circle (64px) with triangle play icon, shadow-brand-lg
- Text overlay: "Product Demo Coming Soon" in white/semi-transparent
- Hover state on play button: scale 1.05 + increased shadow
- The whole video area should feel premium, not like a broken embed
- Use brand shadow-brand-lg on the container

**3 stat cards below video:**
- Horizontal row on desktop (grid-cols-3), stacked on mobile
- Card 1: "50%" + "Lower Overhead" -- icon: dollar sign or chart-down
- Card 2: "17%" + "More Qualified Leads" -- icon: target or filter
- Card 3: "31%" + "Higher Answer Rate" -- icon: phone or arrow-up
- Each card: bg-bg-elevated, rounded-[16px], p-8, shadow-brand-md, border border-border
- Number uses CountUp component (import from @/components/animations/CountUp)
- Number styled large (48px/Outfit/800) in text-primary color
- Label below in text-text-secondary
- Icon above number: simple SVG inline (24px), text-primary color
- Cards animate with StaggerChildren for staggered fade-in on scroll
- Hover: scale 1.02 + shadow elevation increase (use scaleOnHover from animations.ts)

**Brand compliance:**
- Use SectionWrapper with id="how-it-works" (matches nav link target)
- Blue-tinted shadows on all cards
- Pill-shaped elements where appropriate
- Tight letter-spacing on heading
  </action>
  <verify>
    <automated>cd /Users/dsearcher/projects/realestate && npx tsc --noEmit src/components/sections/ProductOverview.tsx 2>&1 | head -20</automated>
  </verify>
  <done>ProductOverview.tsx renders section heading, video placeholder with play button, and 3 stat cards with CountUp animation. Cards have hover states and staggered scroll animation.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ProductOverview into page.tsx replacing how-it-works placeholder</name>
  <files>src/app/page.tsx</files>
  <action>
Read the current page.tsx, add import for ProductOverview, and replace the `<section id="how-it-works" className="min-h-[200px]" />` placeholder with `<ProductOverview />`.

Keep all other imports and sections intact. If SocialProof has already been added by Plan 01, preserve it.

Order: Navbar > Hero > (SocialProof if present) > ProductOverview > remaining placeholders > Footer
  </action>
  <verify>
    <automated>cd /Users/dsearcher/projects/realestate && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>ProductOverview renders on the page at the how-it-works anchor. No TypeScript errors. CountUp animations trigger on scroll.</done>
</task>

</tasks>

<verification>
- `npm run dev` shows Product Overview section with heading, video placeholder, and 3 stat cards
- Stat numbers count up from 0 when scrolled into view
- Cards have staggered fade-in animation
- Play button has hover scale effect
- Responsive: 3-column on desktop, stacked on mobile
</verification>

<success_criteria>
Product overview section renders with "See How It Works" heading, premium video placeholder with play button overlay, and 3 stat cards (50%, 17%, 31%) that count up on scroll entry.
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-sections/03-02-SUMMARY.md`
</output>
