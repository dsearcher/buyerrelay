---
phase: 05-polish-and-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/opengraph-image.tsx
  - src/app/robots.ts
  - src/app/sitemap.ts
  - src/app/page.tsx
  - src/components/sections/Hero.tsx
  - src/components/sections/SocialProof.tsx
  - src/components/sections/ProductOverview.tsx
  - src/components/sections/FeaturesGrid.tsx
  - src/components/sections/CRMIntegrations.tsx
  - src/components/sections/Testimonials.tsx
  - src/components/sections/Comparison.tsx
  - src/components/sections/Pricing.tsx
  - src/components/sections/FinalCTA.tsx
autonomous: true
requirements:
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-04

must_haves:
  truths:
    - "Page title reads 'BuyerRelay - AI-Powered Lead Conversion for Real Estate' and meta description targets 'AI real estate lead generation' keywords"
    - "Sharing the URL on social media shows a branded OG image with title and description"
    - "HTML uses proper heading hierarchy (single h1, section h2s, sub-item h3s) and landmark regions (main, nav, footer already present)"
    - "All images use next/image with width/height props, lazy loading for below-fold, and descriptive alt text"
    - "vercel build succeeds with zero errors"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Complete metadata with OG tags, viewport, theme-color"
      contains: "openGraph"
    - path: "src/app/opengraph-image.tsx"
      provides: "Dynamic OG image for link previews"
      exports: ["default"]
    - path: "src/app/robots.ts"
      provides: "robots.txt generation"
      exports: ["default"]
    - path: "src/app/sitemap.ts"
      provides: "sitemap.xml generation"
      exports: ["default"]
  key_links:
    - from: "src/app/layout.tsx"
      to: "Next.js metadata API"
      via: "export const metadata"
      pattern: "openGraph.*title"
    - from: "src/app/opengraph-image.tsx"
      to: "OG image route"
      via: "Next.js ImageResponse"
      pattern: "ImageResponse"
---

<objective>
Ship the site production-ready with SEO metadata, Open Graph tags, semantic HTML audit, image optimization, and a passing Vercel build.

Purpose: This is the final phase -- making the completed site discoverable by search engines, shareable on social media, accessible, and deployable.
Output: Production-ready site with full SEO metadata, OG image, semantic HTML, optimized images, and clean build.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/layout.tsx
@src/app/page.tsx
@brand_assets/brand-guide.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SEO metadata, Open Graph tags, OG image, and image optimization</name>
  <files>src/app/layout.tsx, src/app/opengraph-image.tsx, src/app/robots.ts, src/app/sitemap.ts</files>
  <action>
1. **Update layout.tsx metadata export** to include comprehensive SEO fields:
   - `title`: "BuyerRelay - AI-Powered Lead Conversion for Real Estate" (already present, verify)
   - `description`: Expand to target keywords -- "BuyerRelay is an AI-powered lead conversion platform for real estate agents. Instantly respond to leads, qualify via AI text and call, nurture automatically, and book 20-50% more appointments."
   - `keywords`: ["AI real estate lead generation", "real estate lead conversion", "AI ISA", "real estate AI calling", "lead qualification automation", "buyer lead nurturing"]
   - `metadataBase`: new URL('https://buyerrelay.com')
   - `openGraph`: { type: 'website', locale: 'en_US', siteName: 'BuyerRelay', title, description, images from auto-generated OG image }
   - `twitter`: { card: 'summary_large_image', title, description }
   - `robots`: { index: true, follow: true }
   - `viewport` is handled by Next.js 15 automatically via `generateViewport` -- add `themeColor: '#0066FF'` via separate `export const viewport` object
   - `icons`: { icon: '/favicon.ico' }

2. **Create src/app/opengraph-image.tsx** using Next.js `ImageResponse` (from `next/og`):
   - 1200x630 dimensions
   - Dark background (#0F172A) with gradient accent strip at top (blue #0066FF to purple #7C3AED)
   - "BuyerRelay" logo text in white (Outfit-style bold)
   - Tagline: "AI-Powered Lead Conversion for Real Estate"
   - Subtle "Book 20-50% More Appointments" stat below
   - Use `export const runtime = 'edge'` and `export const contentType = 'image/png'`
   - Export `size = { width: 1200, height: 630 }` and `alt` string

3. **Create src/app/robots.ts** exporting a default function returning:
   - `{ rules: { userAgent: '*', allow: '/' }, sitemap: 'https://buyerrelay.com/sitemap.xml' }`

4. **Create src/app/sitemap.ts** exporting a default function returning:
   - Single entry: `[{ url: 'https://buyerrelay.com', lastModified: new Date(), changeFrequency: 'weekly', priority: 1 }]`

5. **Image optimization audit**: The site already uses `next/image` for the logo in Navbar and Footer (the only raster images). Verify both have explicit `width` and `height` props. All other visuals are SVG inline or CSS-based, so no further optimization is needed. If any `<img>` tags exist, convert them to next/image.
  </action>
  <verify>
    <automated>cd /Users/dsearcher/projects/realestate && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>layout.tsx has full metadata + OG tags, opengraph-image.tsx generates branded OG image, robots.ts and sitemap.ts exist, all images use next/image, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Semantic HTML audit and production build verification</name>
  <files>src/components/sections/Hero.tsx, src/components/sections/SocialProof.tsx, src/components/sections/ProductOverview.tsx, src/components/sections/FeaturesGrid.tsx, src/components/sections/CRMIntegrations.tsx, src/components/sections/Testimonials.tsx, src/components/sections/Comparison.tsx, src/components/sections/Pricing.tsx, src/components/sections/FinalCTA.tsx, src/app/page.tsx</files>
  <action>
1. **Heading hierarchy audit** -- verify and fix across all sections:
   - Single `<h1>` in Hero.tsx (already correct)
   - Each section uses `<h2>` for its main heading (audit: Hero has h1, ProductOverview/FeaturesGrid/CRMIntegrations/Testimonials/Pricing/FinalCTA should all use h2)
   - Sub-items (feature cards, pricing tiers, footer columns) use `<h3>` (already correct in FeaturesGrid, Pricing, Footer)
   - Comparison.tsx uses h3 for column headers -- add an h2 section heading if missing (e.g., "Ditch the Status Quo" should be h2 not a p/span)
   - No heading level gaps (no h1 -> h3 without h2)

2. **Landmark regions audit**:
   - `<main>` wraps content sections (already in page.tsx)
   - `<nav>` in Navbar (HeroUI Navbar provides this)
   - `<footer>` in Footer.tsx (verify it uses semantic `<footer>` tag)
   - Add `<section>` with `aria-label` to each major content section that doesn't already have one. Each SectionWrapper or direct section div should be a `<section aria-label="[Section Name]">` element. Update SectionWrapper if it renders a `<div>` to render a `<section>` instead, accepting an `ariaLabel` prop.

3. **Alt text audit**:
   - Logo images in Navbar and Footer already have `alt="BuyerRelay"` (verified)
   - SVG icons should have `aria-hidden="true"` (most already do, verify remaining)
   - Any decorative elements should have `aria-hidden="true"` or empty alt

4. **Interactive element audit**:
   - Buttons have accessible labels (testimonial nav buttons already have aria-label)
   - Form inputs in FinalCTA have associated labels or aria-label
   - Ensure focus-visible styles work (Tailwind's focus-visible classes)

5. **Remove leftover comment** in page.tsx: `{/* Phase 4: Conversion sections */}`

6. **Run production build**: `npx next build` -- must complete with zero errors and zero warnings about accessibility or missing alt text.
  </action>
  <verify>
    <automated>cd /Users/dsearcher/projects/realestate && npx next build 2>&1 | tail -30</automated>
  </verify>
  <done>All sections use semantic HTML (section/h2/h3 hierarchy, landmarks, aria-labels), no accessibility warnings in build, production build succeeds cleanly</done>
</task>

</tasks>

<verification>
1. `npx next build` completes with zero errors
2. Inspect build output for correct metadata in rendered HTML
3. OG image route responds at /opengraph-image (can test with `curl -I http://localhost:3000/opengraph-image`)
4. HTML heading hierarchy follows h1 > h2 > h3 with no gaps
5. All sections wrapped in semantic `<section>` with aria-labels
</verification>

<success_criteria>
- Page has title, meta description with "AI real estate lead generation" keywords, and Open Graph tags
- OG image generates a branded 1200x630 image with BuyerRelay branding
- robots.txt and sitemap.xml are generated
- HTML uses semantic heading hierarchy (h1 in hero, h2 per section, h3 for sub-items)
- All landmark regions present (main, nav, footer, section with aria-label)
- All images use next/image with explicit dimensions
- `npx next build` succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-and-deploy/05-01-SUMMARY.md`
</output>
