---
phase: 02-structural-frame
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/Hero.tsx
  - src/components/sections/ChatMockup.tsx
autonomous: true
requirements: [HERO-01, HERO-02, HERO-03, HERO-04, HERO-05, HERO-06, HERO-07]

must_haves:
  truths:
    - "Hero displays 2-column layout with headline+CTAs left and chat mockup right on desktop"
    - "Gradient text effect visible on 'Closed Deals' in headline"
    - "Two CTA buttons render: 'Book a Demo' primary pill and 'See It In Action' outline pill"
    - "Chat mockup auto-types a lead qualification conversation with sequential bubble animations"
    - "'Try the AI' phone input appears below chat mockup"
    - "Hero has subtle radial gradient background with visual texture"
    - "Hero stacks vertically on mobile with chat mockup below headline"
  artifacts:
    - path: "src/components/sections/Hero.tsx"
      provides: "2-column hero layout with gradient bg, headline, CTAs, phone input"
      min_lines: 80
    - path: "src/components/sections/ChatMockup.tsx"
      provides: "Animated AI chat conversation with typing effect"
      min_lines: 100
  key_links:
    - from: "src/components/sections/Hero.tsx"
      to: "src/components/sections/ChatMockup.tsx"
      via: "import and render in right column"
      pattern: "import.*ChatMockup"
    - from: "src/components/sections/ChatMockup.tsx"
      to: "Motion animation"
      via: "useEffect timer chain for sequential messages"
      pattern: "useEffect|setTimeout|setInterval"
---

<objective>
Build the hero section -- the highest-impact visual on the page. Includes 2-column layout with gradient headline + CTAs on the left and an animated AI chat mockup on the right that demonstrates BuyerRelay's lead qualification in action.

Purpose: Cold email recipients land here first. The chat mockup is the primary "show don't tell" element that proves the AI works. Must feel premium, not template-ish.
Output: Hero.tsx (layout + background + CTAs), ChatMockup.tsx (animated conversation)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-structural-frame/02-CONTEXT.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@src/app/globals.css
@src/lib/animations.ts
@src/components/animations/ScrollReveal.tsx
@src/types/index.ts
@brand_assets/brand-guide.md

<interfaces>
<!-- Key types and animation utilities the executor needs -->

From src/lib/animations.ts:
```typescript
export const fadeInUp = { initial: { opacity: 0, y: 30 }, whileInView: { opacity: 1, y: 0 }, ... };
export const fadeIn = { initial: { opacity: 0 }, whileInView: { opacity: 1 }, ... };
export const slideInLeft = { initial: { opacity: 0, x: -40 }, whileInView: { opacity: 1, x: 0 }, ... };
export const slideInRight = { initial: { opacity: 0, x: 40 }, whileInView: { opacity: 1, x: 0 }, ... };
```

From src/components/animations/ScrollReveal.tsx:
```typescript
// ScrollReveal({ children, direction?, delay?, className? })
// direction: 'up' | 'left' | 'right' | 'fade'
```

From src/app/globals.css:
```css
.gradient-text { background: linear-gradient(90deg, #0066FF, #7C3AED); -webkit-background-clip: text; ... }
/* Tokens: --color-primary: #0066FF, --color-accent: #7C3AED, --color-bg-primary: #FAFBFF */
/* --radius-pill: 999px, --shadow-brand-md, --shadow-brand-lg */
```

From brand_assets/brand-guide.md (H1 specs):
- 56px / Outfit 800 / letter-spacing -0.03em / line-height 1.1
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Hero layout with gradient background, headline, and CTAs</name>
  <files>src/components/sections/Hero.tsx</files>
  <action>
Create `src/components/sections/Hero.tsx` as a 'use client' component (needs Motion for animations).

**Background layer:**
The entire hero section gets a subtle radial gradient background. Use a relative container with an absolute pseudo-element or div:
- Radial gradient from center: `radial-gradient(ellipse at 30% 20%, rgba(0,102,255,0.08) 0%, transparent 60%)`
- Second radial: `radial-gradient(ellipse at 70% 80%, rgba(124,58,237,0.06) 0%, transparent 50%)`
- Subtle dot grid texture via CSS: `background-image: radial-gradient(circle, rgba(0,102,255,0.04) 1px, transparent 1px); background-size: 24px 24px;`
- Apply these on an absolute div behind content with pointer-events-none.

**Layout:**
- Outer: `min-h-[calc(100vh-64px)]` (full viewport minus navbar), flex items-center
- Inner: Use max-w-[1280px] mx-auto px-4 sm:px-6 lg:px-8, grid grid-cols-1 lg:grid-cols-[55%_45%] gap-12 lg:gap-8 items-center
- Top padding: pt-16 md:pt-24 to give breathing room below navbar

**Left column (headline + CTAs):**
- Animate with ScrollReveal direction="up" or Motion fadeInUp
- Small eyebrow badge above headline: "AI-Powered Lead Conversion" in a pill-shaped span (bg-primary/10 text-primary text-sm font-medium px-4 py-1.5 rounded-pill inline-block mb-6)
- H1: "Turn Cold Leads Into <span class='gradient-text'>Closed Deals</span>" — font-heading text-4xl sm:text-5xl lg:text-[56px] font-extrabold tracking-[-0.03em] leading-[1.1]
- Subtitle paragraph below H1: "BuyerRelay instantly responds to your leads, qualifies them via AI text and call, nurtures automatically, and delivers ready-to-close buyers straight to your calendar." — text-text-secondary text-lg md:text-xl leading-relaxed mt-6 max-w-[540px]
- CTA row (flex gap-4 mt-8):
  - "Book a Demo" — button with bg-primary text-white font-medium px-8 py-3.5 rounded-pill shadow-brand-md hover:shadow-brand-lg hover:scale-[1.02] transition-transform transition-shadow. Link to #pricing or #cta section.
  - "See It In Action" — button with border-2 border-primary text-primary font-medium px-8 py-3.5 rounded-pill hover:bg-primary/5 transition-colors. Scrolls to #how-it-works.
- Small trust line below CTAs: "Trusted by 500+ real estate agents" with a subtle check icon — text-text-muted text-sm mt-6

**Right column (chat mockup slot):**
- Animate with ScrollReveal direction="right" delay={0.2}
- Import and render `<ChatMockup />` (created in Task 2)
- Below the ChatMockup, add the "Try the AI" phone input:
  - A flex row: input (type="tel", placeholder="Enter your phone number", rounded-l-pill border border-border px-5 py-3.5 text-sm w-full max-w-[280px] focus:outline-none focus:ring-2 focus:ring-primary/30 focus:border-primary) + button ("Try It" in a bg-gradient-to-r from-primary to-accent text-white px-6 py-3.5 rounded-r-pill font-medium hover:shadow-brand-md transition)
  - This is a placeholder: onClick scrolls to #cta section
  - Label above: "See how it works" text-text-muted text-xs mt-4 mb-2

**Mobile responsive (HERO-07):**
- grid-cols-1 on mobile stacks vertically — headline first (order-1 on mobile), chat mockup second (order-2)
- On lg breakpoints: grid-cols-[55%_45%] with natural order
- Phone input goes full-width on mobile (max-w-full)
  </action>
  <verify>
    <automated>cd /Users/dsearcher/projects/realestate && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Hero section renders with gradient background texture, eyebrow badge, H1 with gradient "Closed Deals" text, subtitle, two CTA buttons (primary pill + outline pill), trust line, and a slot for ChatMockup with phone input below. Responsive: stacks on mobile, 55/45 grid on desktop. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Build animated ChatMockup with typing effect and wire into page</name>
  <files>src/components/sections/ChatMockup.tsx, src/app/page.tsx</files>
  <action>
Create `src/components/sections/ChatMockup.tsx` as a 'use client' component.

**Visual frame:**
Use a floating card design (not iPhone frame — cleaner, more modern):
- Outer container: bg-bg-elevated rounded-[20px] shadow-brand-xl border border-border/50 overflow-hidden
- Header bar: px-5 py-3 border-b border-border/50 flex items-center gap-3
  - Green dot (w-2.5 h-2.5 rounded-full bg-success) + "BuyerRelay AI" text (font-heading font-semibold text-sm) + small "Online" badge (text-xs text-success)
- Chat body: px-5 py-4 space-y-3 min-h-[320px] md:min-h-[380px] bg-[#F8FAFC]

**Conversation messages (hardcoded array):**
Define a messages array with the conversation script:
```typescript
const conversation = [
  { sender: 'ai', text: "Hi Sarah! I'm reaching out from BuyerRelay about your home search in Austin. Are you still looking? 🏠", delay: 0 },
  { sender: 'lead', text: "Yes! We're looking for a 3-bed in South Austin under $450K", delay: 1200 },
  { sender: 'ai', text: "Great taste! South Austin is hot right now. What's your timeline — are you pre-approved yet?", delay: 2400 },
  { sender: 'lead', text: "We got pre-approved last week actually", delay: 3800 },
  { sender: 'ai', text: "Perfect — you're a serious buyer! Let me connect you with an agent who specializes in South Austin. Can I book you a call for tomorrow at 2pm? 📅", delay: 5000 },
  { sender: 'lead', text: "That works!", delay: 6200 },
  { sender: 'ai', text: "Done! You'll get a calendar invite shortly. Talk soon, Sarah! ✨", delay: 7200 },
];
```

**Animation logic:**
- Use `useState` to track `visibleMessages` (number, starts at 0)
- Use `useEffect` with a chain of `setTimeout` calls based on each message's delay
- Each message appears with a brief typing indicator first (3 animated dots), then the message fades in
- Typing indicator: Show for 600ms before each AI message (not for lead messages — they appear immediately at their delay)
- Use `useRef` for cleanup of timeouts on unmount

**Message bubble styling:**
- AI messages: bg-white rounded-2xl rounded-tl-sm px-4 py-2.5 text-sm text-text-primary shadow-brand-sm max-w-[85%] — aligned left
- Lead messages: bg-primary text-white rounded-2xl rounded-tr-sm px-4 py-2.5 text-sm max-w-[85%] — aligned right (ml-auto)
- Each bubble enters with a subtle scale+opacity animation using Motion: `initial={{ opacity: 0, scale: 0.95, y: 10 }} animate={{ opacity: 1, scale: 1, y: 0 }}`
- Stagger from bottom of container

**Typing indicator:**
- Three dots that pulse: use CSS animation (opacity keyframes 0%->100%->0% on each dot with 0.2s stagger)
- Styled same as AI bubble but with the dots instead of text
- Only visible briefly before each AI message

**Auto-scroll:**
- Use a ref on the chat body container, scroll to bottom when new messages appear: `containerRef.current?.scrollTo({ top: containerRef.current.scrollHeight, behavior: 'smooth' })`

**Wire into page.tsx:**
Update `src/app/page.tsx` to import Hero:
```tsx
import { Navbar } from '@/components/sections/Navbar';
import { Hero } from '@/components/sections/Hero';
import { Footer } from '@/components/sections/Footer';

export default function HomePage() {
  return (
    <>
      <Navbar />
      <main>
        <Hero />
        <section id="how-it-works" className="min-h-[200px]" />
        <section id="features" className="min-h-[200px]" />
        <section id="pricing" className="min-h-[200px]" />
      </main>
      <Footer />
    </>
  );
}
```

Note: If Plan 01 has already modified page.tsx, merge the Hero import into the existing structure (Navbar and Footer should already be there). If Plan 01 hasn't run yet, include all three imports.
  </action>
  <verify>
    <automated>cd /Users/dsearcher/projects/realestate && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>ChatMockup displays animated conversation with sequential message bubbles, typing indicators before AI messages, and auto-scroll. Hero section renders complete with ChatMockup in right column and phone input below. page.tsx imports and renders Navbar + Hero + Footer. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run dev` shows full hero section: gradient background, headline with gradient text, two CTAs, chat mockup with animated conversation
- Chat mockup messages appear sequentially with typing dots before AI messages
- Phone input renders below chat mockup with "Try It" button
- Desktop: 55/45 two-column layout
- Mobile (375px): headline stacks above chat mockup
- "Book a Demo" and "See It In Action" buttons are pill-shaped with correct brand colors
- Build completes with no TypeScript or build errors
</verification>

<success_criteria>
- Hero has radial gradient background with dot texture
- H1 shows "Turn Cold Leads Into Closed Deals" with gradient on "Closed Deals"
- Two CTA buttons render as pills (primary solid + outline)
- Chat mockup auto-plays 7-message lead qualification conversation
- Typing indicator (3 dots) shows before each AI message
- Phone input with "Try It" button appears below mockup
- Layout is 55/45 on desktop, stacked on mobile
- Full page renders: Navbar -> Hero -> placeholder sections -> Footer
</success_criteria>

<output>
After completion, create `.planning/phases/02-structural-frame/02-02-SUMMARY.md`
</output>
